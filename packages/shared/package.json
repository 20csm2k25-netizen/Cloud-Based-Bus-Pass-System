{
  "name": "@routewise/shared",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"shared tests not yet added\""
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}